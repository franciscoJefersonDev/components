{
  "name": "dropdown",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "sass --style=compressed --update src/Dropdowns.scss finish/dropdowns.css && tsc --target esnext src/Dropdowns.ts --outdir finish && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sass": "^1.53.0",
    "typescript": "^4.5.4",
    "vite": "^2.9.9"
  }
}